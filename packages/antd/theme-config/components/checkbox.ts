export const genCheckboxToken  = (colors: Record<string, string>) => {
  return {
    "Checkbox": {
      'colorBorder': colors['cbd-gray-3'],
      'colorTextDisabled': colors['cbd-gray-4'],
      'lineWidth': 2,
    }
  }
}
