export const genCascaderToken  = (colors: Record<string, string>) => {
  return {
    "Cascader": {
      'colorBorder': colors['cbd-gray-3'],
      'colorSplit': colors['cbd-gray-3'],
      'colorHighlight': colors['cbd-brand-4'],
      'colorText': colors['cbd-gray-7'],
    }
  }
}
