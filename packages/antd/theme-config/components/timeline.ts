export const genTimelineToken  = (colors: Record<string, string>) => {
  return {
    "Timeline": {
      'colorText': colors['cbd-gray-7'],
      'colorTextDisabled': colors['cbd-gray-4'],
      'tailColor': colors['cbd-gray-3'],
    }
  }
}
