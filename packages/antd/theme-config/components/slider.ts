export const genSliderToken  = (colors: Record<string, string>) => {
  return {
    "Slider": {
      'railBg': colors['cbd-gray-3'],
      'handleColor': colors['cbd-brand-4'],
      'trackBg': colors['cbd-brand-4'],
    }
  }
}
