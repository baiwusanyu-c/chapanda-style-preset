export const genStatisticToken  = (colors: Record<string, string>) => {
  return {
    "Statistic": {
      "colorTextDescription": colors['cbd-gray-6'],
      "colorTextHeading": colors['cbd-gray-7'],
    }
  }
}
