export const genModalToken  = (colors: Record<string, string>) => {
  return {
    "Modal": {
      'titleColor': colors['cbd-gray-7'],
      'colorText': colors['cbd-gray-7'],
      'borderRadiusLG': 2,
      'borderRadiusSM': 2,
    }
  }
}
